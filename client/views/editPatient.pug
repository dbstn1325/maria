extends base.pug

block content
    if (errorMessage)
        span errorMessage
    form(method="POST")
        input(type="text", placeholder="성함", name ="patientName", value=`${items.NAME}`)
        input(type="text", placeholder="희망진료명", name ="disease", value=`${items.DISEASE}`)
        input(type = "submit", value="수정완료")